import { SectionWrapper } from "@/components";

function FeaturesNewsArticles() {
  return (
    <SectionWrapper className="bg-primary h-screen">
      Features section
    </SectionWrapper>
  );
}

export default FeaturesNewsArticles;
